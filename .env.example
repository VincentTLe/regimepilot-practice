# Alpaca PAPER trading keys — https://app.alpaca.markets/paper/dashboard/overview
# Never put live keys here. This project refuses to run against live trading.
ALPACA_API_KEY=PK...your-paper-key...
ALPACA_SECRET_KEY=...your-paper-secret...

# Must stay "true". Any other value aborts startup (paper-only project).
ALPACA_PAPER=true

# Whitelist of candidate underlyings, comma-separated (optional; this is the default).
SYMBOLS=SPY,QQQ,IWM,AAPL,NVDA,TSLA,MSFT,AMZN

# Bar duration for momentum features: 5m, 15m, 1h, 1d, 1w, ... (optional; default 15m).
BAR_TIMEFRAME=15m

# Optional: only needed for LLM decisions — https://openrouter.ai/
# Leave blank and run with --manual-mode if you don't have one.
OPENROUTER_API_KEY=
